---
group: platform
component: Framework
impact: none
category: Improved
---

## Summary

The human documentation now teaches NeoHaskell from first concepts through
application building, integrations, AI features, deployment, and recovery.
An ecommerce practice project connects the examples; readers can apply the concepts
to their own applications or consult individual topics. Each page starts with an
understandable problem and develops the implementation details gradually.

The introduction explains the philosophy in depth before code, using diagrams to
connect intent, history, corrections, feature boundaries, and working with an agent.
Worked event-model diagrams and real IDE screenshots show how requests, facts,
and read models relate, from a first cart to a selected command's fields.
Select an image to enlarge it in a dialog; close it to continue reading in place.

Install the released Neo CLI and create your own project once with `neo new`.
Build the cart there, explore it in the IDE, and keep growing the same application
with `neo build`, `neo run`, and `neo test` throughout the guide.

The website adds in-page image enlargement and a runnable learning journey;
application behaviour and public APIs are unchanged by this documentation work. Begin at the website's Start and evaluate section, or use the
capability reference to find a specific topic.
