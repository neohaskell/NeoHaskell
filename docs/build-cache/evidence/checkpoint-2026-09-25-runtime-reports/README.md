Replay of the existing report validator at176a5bf against actual Linux service reports from36155654547 and36155658355, archived in631d9c1. All19 registered service boundary selectors pass per report; in-memory selector removal and fixture-marker removal are rejected for every entry. No repository expectations or reports are changed. This proves compatibility of the component-produced reports with the existing validator beyond the earlier zero-locator PR-diff check. It is not a hosted criteria-runtime step invocation: workflow_dispatch still intentionally skips that PR-only step.

Reproduce from the recorded checkout with `python3 /path/to/replay.py /path/to/first/.nhcore-test-service.report /path/to/second/.nhcore-test-service.report`. The reports are in the current full-CI archive at631d9c1; hashes in results.json verify exact input.
