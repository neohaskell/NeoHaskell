# Service framework — Codex scope

Follow the repository [agent contract](../../AGENTS.md) and the parent
[core scope](../AGENTS.md). The event-sourcing and CQRS framework uses the
NeoHaskell dialect and spec-gated pipeline.

Localize through the repository codemap and `neohaskell-localizer`; implement
with `neohaskell-implementer`. Run `./dev` commands from the repository root.
