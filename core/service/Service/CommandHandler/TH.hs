-- | DEPRECATED: Use Service.CommandExecutor.TH instead.
--
-- This module is kept for backward compatibility during the transition.
module Service.CommandHandler.TH (
  command,
  deriveKnownHash,
) where

import Service.CommandExecutor.TH (command, deriveKnownHash)
