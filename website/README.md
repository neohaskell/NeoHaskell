# Website moved

The public website now lives in [`neohaskell/website-v2`](https://github.com/neohaskell/website-v2).

The standalone repository fetches the latest `main` commit from this repository before running documentation evidence checks.
