# Reference app and acceptance tests — Codex scope

Follow the repository [agent contract](../AGENTS.md), including the NeoHaskell
dialect, localization, and spec-gated pipeline. Use `neohaskell-localizer` and
`neohaskell-implementer`; acceptance coverage belongs to the testbed contract.

Canonical skills live in [.agents/skills](../.agents/skills/).
Run `./dev` commands from the repository root; preserve existing expectations.
